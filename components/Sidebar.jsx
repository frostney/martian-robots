const React = require('react');
const { Box, Text } = require('ink');

const Sidebar = () => {
  return (
    <Box borderStyle="round">
      <Text>Output</Text>
    </Box>
  );
};

module.exports = Sidebar;
